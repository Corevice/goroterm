• Desktop: opening a tmux session now appears as a tab in the same window as the connection, instead of a separate window.
• Desktop: command-finished notifications now fire when the window is unfocused (like mobile).
• Fix: Settings now shows the real app version.
