• New: set a "Claude system prompt file" per connection. On that server, running claude automatically adds --system-prompt-file (whether typed or via the button). Leave empty for the normal claude command.
