• New: Port Tunnels — open SSH local port forwards to Docker containers (or any host the SSH server can reach) from the new "Tunnels" tab in the right drawer. Multiple tunnels run in parallel and auto-restore on reconnect.
